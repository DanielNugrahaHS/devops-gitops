# templates/secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Chart.Name }}-secret
type: Opaque
data:
  rabbitmq-username: {{ .Values.auth.username | b64enc }}
  rabbitmq-password: {{ .Values.auth.password | b64enc }}
  rabbitmq-erlang-cookie: {{ .Values.auth.erlangCookie | b64enc }}